<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item android:id="@+id/mock_qr_code_reading"
        android:icon="@drawable/ic_launcher"
        android:title="@string/toolbar_activate_edit_mode"
        app:showAsAction="always"
        android:visible="true"
        />

    <item android:id="@+id/mock_questionnaire"
        android:icon="@drawable/ic_action_action_list"
        android:title="@string/toolbar_search"
        app:showAsAction="always"
        android:visible="true"
        />
</menu>